{
  "name": "hoodfactory-whale-radar",
  "version": "1.0.0",
  "private": true,
  "description": "HOODFACTORY website plus Whale Radar (whale accumulation detection via the Blockscout Pro API)",
  "scripts": {
    "build": "npm run build:privy",
    "build:privy": "esbuild src/privy-wallet.jsx --bundle --minify --format=iife --target=es2020 --outfile=privy-wallet.js"
  },
  "dependencies": {
    "@privy-io/react-auth": "^3.35.2",
    "ethers": "^6.15.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  },
  "overrides": {
    "axios": "1.18.1",
    "uuid": "11.1.1",
    "ws": "8.21.1"
  }
}
